#! /bin/bash
# this is a simple function example
echo "startig the function definition..."
funcExample () {
    echo "we are now INSIDE the function..."
}
    echo "NOW let's call it"
# call the function
        echo "...and back outside the function, continuing to the next command."
    echo "That's it"
